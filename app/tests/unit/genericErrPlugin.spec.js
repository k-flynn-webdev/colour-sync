import { expect } from 'chai'
import { shallowMount } from '@vue/test-utils'
import GenericErrPlugin from '@/plugins/genericErrPlugin.js'

// todo more tests
describe('GenericErrPlugin.js', () => {
  describe('Mixin mode', () => {
    it('throws error')
    it('shows error from a `response.data` object')
    it('shows basic error from a `response.detail` object')
    it('shows basic error from a `response.message` object')
  })
})
