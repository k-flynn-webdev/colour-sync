import { expect } from 'chai'
import { shallowMount } from '@vue/test-utils'
import MessagePlugin from '@/plugins/messagePlugin.js'

// todo more tests
describe('MessagePlugin.js', () => {
  it('`generateId` returns a hash from a string')
  it('`generateId` returns the same hash from a string')
  it('`generateId` returns a hash from null')
  it('`createMessage` returns true when sending a new message')
  it('`createMessage` returns false when sending a duplicate message')
  it('`removeMessage` removes a message that exists')
  it('`removeMessage` does not removes a message that doesnt exists')
})
